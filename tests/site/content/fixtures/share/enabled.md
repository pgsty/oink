---
title: Shared page
description: Four inherited targets - one intent link per URL shape, plus the local copy control.
weight: 10
---

The bar above the feedback prompt is the section's inherited list: `x` and
`hackernews` carry the URL and the title, `email` is a `mailto:` that carries
the title first, and `copy` is the local `copy_link` action rather than a link.
